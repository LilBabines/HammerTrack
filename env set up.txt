-------- PYTHON VERSION : 3.13 ------------------

1.    pip install torch torchvision
1.    pip3 install  ultralytics pyside6 boxmot
2.    cd src/
3.    git clone https://github.com/facebookresearch/sam2.git && cd sam2
4.    pip install -e .
5.    cd checkpoints && ./download_ckpts.sh

some conflicts between openCV and pyside may occur, on ubuntu `sudo apt install libxcb-cursor0` fix it
